import { Ready2ShopValidators } from './ready2-shop-validators';

describe('Ready2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new Ready2ShopValidators()).toBeTruthy();
  });
});
